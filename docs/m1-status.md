# M1 status

## Completed

- bounded read-only HTTP collection;
- final URL and redirect-chain evidence;
- response duration and page title;
- public-address validation for the initial target and redirect hops;
- basic DNS evidence;
- structured timeout and request errors;
- integration with run history, change detection, action control, and reports;
- Linux and Windows CI proof.

## Remaining

- TLS certificate-expiry collection;
- transport-level address pinning to fully close the DNS validation-to-connection gap;
- metadata checks beyond the page title;
- a sanitized live demonstration report committed as portfolio evidence.

The repository must not claim these remaining items as implemented until their tests and CI proof pass.
